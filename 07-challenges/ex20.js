/**
 * Ex20 — Mini EventEmitter
 * TODOs:
 * - Methods: on(event, fn), off(event, fn), emit(event, payload)
 * - Optional: once(event, fn)
 */

class EventEmitter {
  // TODO
}

// TODO: tests for subscribe, off, emit
